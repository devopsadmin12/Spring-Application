pipeline { 
  agent any
  tools {
    maven 'Maven'
  }
  stages {
    stage ('SCM Checkout') {
      steps {
      git 'https://github.com/devopsadmin12/Spring-Application.git'
      }
    }
  
  }
}
